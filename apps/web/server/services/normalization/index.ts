/**
 * Purpose: Public API barrel file for the Product Normalization Engine.
 * Exports the types, constants, and the main `normalizeProduct` entry point 
 * to ensure encapsulation of the internal folder structure from the rest of the backend.
 */
export * from './types';
export * from './constants';
export * from './normalizeProduct';
